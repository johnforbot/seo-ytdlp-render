services:
  - type: web
    name: tubekit-ytdlp
    env: docker
    plan: starter
    dockerfilePath: ./Dockerfile
    dockerContext: .
    autoDeploy: true
    healthCheckPath: /health
    envVars:
      - key: YTDLP_SERVICE_TOKEN
        sync: false